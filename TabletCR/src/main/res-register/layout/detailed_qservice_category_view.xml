<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@id/container"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="@dimen/category_list_item_height"
    android:gravity="center_vertical"
    android:background="@drawable/detailed_qs_categories_selector">

    <ImageView
        android:id="@+id/image"
        android:layout_height="match_parent"
        android:layout_width="wrap_content"
        android:layout_gravity="center_vertical"
        android:background="@drawable/category_image_bg"
        tools:src="@drawable/categories_placeholder"
        android:paddingStart="10dp"
        android:paddingLeft="10dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp" />

    <TextView
        style="@style/DrawerPicker.ItemText.DetailedQSCategory"
        android:id="@+id/title"
        tools:text="asdasdas"
        android:gravity="center_vertical"
        android:layout_height="match_parent"
        android:layout_width="wrap_content"
        android:paddingLeft="12dp"
        android:paddingStart="12dp" />

</LinearLayout>