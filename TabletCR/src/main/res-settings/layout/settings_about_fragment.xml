<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="16dp"
                android:paddingRight="16dp"
                android:paddingTop="6dp"
                android:paddingBottom="8dp">


    <TextView
        android:id="@+id/data_value"
        android:layout_marginTop="4dp"
        android:layout_alignParentRight="true"
        style="@style/Settings.Fragments.SubTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/release_note"
        android:layout_below="@+id/data_value"
        android:layout_marginTop="4dp"
        android:layout_alignParentRight="true"
        style="@style/Settings.Fragments.SubTitle"
        android:text="@string/about_release_note"
        android:textSize="14sp"
        android:textColor="@color/refund_list_selected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/data_label"
        android:layout_marginTop="4dp"
        android:layout_toLeftOf="@id/data_value"
        style="@style/Settings.Fragments.SubTitle"
        android:text="@string/about_version_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>


</RelativeLayout>